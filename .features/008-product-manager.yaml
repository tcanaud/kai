feature_version: "1.0"

# ── Identity ──────────────────────────────────────────
feature_id: "008-product-manager"
title: "Product Manager"
status: "active"
owner: "tcanaud"
created: "2026-02-18"
updated: "2026-02-18"

# ── Dependencies ──────────────────────────────────────
depends_on: []
tags: []

# ── Workflow ──────────────────────────────────────────
workflow_path: "full"

# ── Lifecycle (computed) ──────────────────────────────
lifecycle:
  stage: "implement"
  stage_since: "2026-02-18"
  progress: 0.96
  manual_override: null

# ── Artifacts (computed from scan) ────────────────────
artifacts:
  bmad:
    prd: true
    architecture: false
    epics: false
  speckit:
    spec: true
    plan: true
    research: true
    tasks: true
    contracts: true
    tasks_done: 22
    tasks_total: 23
  agreement:
    exists: false
    status: ""
    check: "MISSING"
  adr:
    count: 0
    ids: []
  mermaid:
    count: 0
    layers:
      L0: 0
      L1: 0
      L2: 0

# ── Health (computed) ─────────────────────────────────
health:
  overall: "WARNING"
  agreement: "MISSING"
  spec_completeness: 1.0
  task_progress: 0.96
  adr_coverage: 0
  diagram_coverage: 0
  warnings:
    - "No ADRs reference this feature"
    - "adr_coverage == 0"

# ── Regression Detection ─────────────────────────────
last_scan:
  timestamp: "2026-02-18T22:00:00Z"
  stage: "implement"
  artifacts_snapshot:
    bmad_prd: true
    speckit_spec: true
    speckit_plan: true
    speckit_tasks: true
    agreement_exists: false
